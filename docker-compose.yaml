services:
  db:
    image: postgres:18
    container_name: soloconference_db
    env_file:
      - ./backend/database.env
    ports:
      - "5432:5432"
    volumes:
      - soloconference_db_data:/var/lib/postgresql
    networks:
      - soloconference_network


networks:
  soloconference_network:
    driver: bridge

volumes:
  soloconference_db_data:
